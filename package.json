{
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "gh-pages": "^6.2.0",
    "parcel": "^2.12.0",
    "sass": "^1.50.0"
  },
  "scripts": {
    "start": "parcel *.html --no-cache --watch-dir /",
    "build": "parcel build *.html --public-url /",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
